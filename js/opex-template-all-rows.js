const KEY='dadBudgetOPEXBaselineV17';
const MONTHS=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
const COUNTRIES=['Jordan','Saudi Arabia','United Arab Emirates','Qatar','Bahrain','Kuwait','Oman','Iraq','Algeria','Egypt','Lebanon','Palestine','Yemen','Morocco','Tunisia','Libya','Sudan','Syria','Turkey','United States','United Kingdom','France','Germany','Italy','Spain','Switzerland','Netherlands','Belgium','Austria','Greece','Cyprus','India','Pakistan','Bangladesh','Sri Lanka','China','Japan','South Korea','Singapore','Malaysia','Indonesia','Philippines','Thailand','Australia','New Zealand','Canada','Brazil','South Africa','Kenya','Nigeria','Ghana','Ethiopia'];
const n=v=>{const x=Number(v||0);return Number.isFinite(x)?x:0};
function loadModel(){try{const m=JSON.parse(localStorage.getItem(KEY)||'null');if(m?.departments)return m}catch(_){}const keys=Object.keys(localStorage).filter(k=>/^dadBudgetOPEXBaselineV\d+$/i.test(k)).sort((a,b)=>Number((b.match(/\d+$/)||[0])[0])-Number((a.match(/\d+$/)||[0])[0]));for(const k of keys){try{const m=JSON.parse(localStorage.getItem(k)||'null');if(m?.departments)return m}catch(_){}}return null}
function controlled(code){const x=Number(String(code||'').trim());return (x>=6010001&&x<=6010031)||(x>=6020001&&x<=6020010)||(x>=6030010&&x<=6030180&&x%10===0)}
function category(code){const c=String(code||''),p=c.slice(0,3),m={'601':'Employees Benefits','602':'Travel Costs','603':'Depreciation and Amortization','604':'Maintenance cost','605':'A&P, Marketing Activities','606':'IT and Connectivity Expenses','607':'Professional & Consultation Expenses','608':'Utilities Expenses','609':'Insurance Expenses','610':'Logistic Expenses','611':'Governmental and Taxes Expenses','612':'Vehicles Expenses','613':'Products related Expense','614':'Other Expenses'};if(c==='6050015'||c==='6050016')return'Other Expenses';if(c==='6140019')return'Products related Expense';return m[p]||'Other Expenses'}
function actualYTD(x){return Object.values(x?.actualByMonth||{}).reduce((s,v)=>s+n(v),0)+n(x?.actualUnperiodized)}
function selectedRange(){const from=document.getElementById('dateFrom')?.value||document.getElementById('from')?.value||localStorage.getItem('dadBudgetOPEXDateFrom')||'2026-01',to=document.getElementById('dateTo')?.value||document.getElementById('to')?.value||localStorage.getItem('dadBudgetOPEXDateTo')||'2026-07';return{from,to}}
function sumRange(map,from,to){return Object.entries(map||{}).reduce((s,[key,value])=>key>=from&&key<=to?s+n(value):s,0)}
function referenceValues(item){const range=selectedRange(),hasActual=Object.keys(item?.actualByMonth||{}).length>0,priorFrom=`${Number(range.from.slice(0,4))-1}${range.from.slice(4)}`,priorTo=`${Number(range.to.slice(0,4))-1}${range.to.slice(4)}`,budget=sumRange(item?.budgetByMonth,range.from,range.to),actual=hasActual?sumRange(item?.actualByMonth,range.from,range.to):n(item?.actualUnperiodized),ly=sumRange(item?.lyByMonth,priorFrom,priorTo)+(Object.keys(item?.lyByMonth||{}).length?0:n(item?.lyUnperiodized)),fy=n(item?.fyBudget);return{ly,budget,actual,fy}}
function allAccounts(m,d){
  const merged=new Map();
  Object.values(m.accountMaster||{}).forEach(a=>{const code=String(a?.code||'').trim();if(code)merged.set(code,{code,name:String(a?.name||code).trim()||code})});
  Object.values(d.items||{}).forEach(a=>{const code=String(a?.code||'').trim();if(!code)return;const prev=merged.get(code)||{};merged.set(code,{code,name:String(a?.name||prev.name||code).trim()||code})});
  return [...merged.values()]
    .filter(a=>/^6(01|02|03|04|05|06|07|08|09|10|11|12|13|14)/.test(String(a.code))&&!controlled(a.code))
    .sort((a,b)=>String(a.code).localeCompare(String(b.code)));
}
async function downloadAllRows(){
  const m=loadModel(),cc=document.getElementById('deptFilter')?.value,d=m?.departments?.[cc];
  if(!m||!d){alert('Upload the Finance OPEX baseline first, then select a department.');return}
  if(typeof ExcelJS==='undefined'){alert('Excel template engine is still loading. Please try again in a few seconds.');return}
  const wb=new ExcelJS.Workbook();wb.creator='DAD Budget 2027';wb.created=new Date();wb.calcProperties.fullCalcOnLoad=true;wb.calcProperties.forceFullCalc=true;
  const info=wb.addWorksheet('Instructions');[
    ['DAD BUDGET 2027 - DEPARTMENT TEMPLATE'],['Department',d.name],['Fund Center',d.cc],[],['HOW TO USE'],
    ['1','Enter Landing 2026 and Budget 2027 only in the green cells. All reference and formula columns are locked.'],
    ['2','FY Landing calculates automatically as Actual YTD plus Landing for the remaining 2026 months.'],
    ['3','All available OPEX accounts are included even when Actual YTD 2026 and FY Budget 2026 are zero.'],
    ['4','Employee Benefits G/L 6010001-6010031 are controlled by the separate HR Budget model.'],
    ['5','Travel G/L 6020001-6020010 are controlled by the Travel Budget sheet.'],
    ['6','Depreciation and Amortization G/L 6030010-6030180 are controlled by a separate model.'],
    ['7','Professional & Consultation G/L 607 accounts are controlled by the Professional Consultation sheet. Details are mandatory for every entered amount.'],
    ['8','Travel Budget dropdowns are provided for Department, Title, Country, Month and Number of Nights.'],
    ['9','Upload this same workbook once after completion.']
  ].forEach(r=>info.addRow(r));info.getColumn(1).width=18;info.getColumn(2).width=90;

  const op=wb.addWorksheet('OPEX Budget 2027'),range=selectedRange(),numberFormat='#,##0;[Red]-#,##0;–';
  op.addRow(['LANDING 2026 & FY BUDGET 2027 · MONTHLY DISTRIBUTION']);op.mergeCells('A1:X1');
  op.getCell('A1').font={bold:true,size:16,color:{argb:'FFFFFFFF'}};op.getCell('A1').fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF082765'}};op.getCell('A1').alignment={vertical:'middle',horizontal:'left'};op.getRow(1).height=30;
  op.addRow(['Fund Center','G/L Account','Account Name','Category','LY YTD','Budget YTD','Actual','Landing','FY Landing','FY Budget 2026',...MONTHS,'FY Budget 2027','Source / Note']);
  const header=op.getRow(2);header.height=30;header.eachCell(cell=>{cell.font={bold:true,color:{argb:'FFFFFFFF'}};cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF0A2C61'}};cell.alignment={vertical:'middle',horizontal:'center',wrapText:true};cell.border={bottom:{style:'thin',color:{argb:'FF4CCFC4'}},right:{style:'thin',color:{argb:'FF315782'}}}});
  header.getCell(8).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF087F77'}};header.getCell(8).note='Expected expense for the remaining months of 2026 · Department input';
  for(let c=11;c<=22;c++){const cell=header.getCell(c);cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF087F77'}};cell.note=`${MONTHS[c-11]} 2027 · Department input`}
  header.getCell(23).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF0B665F'}};
  const accounts=allAccounts(m,d),professionalAccounts=accounts.filter(a=>String(a.code).startsWith('607')),groups=new Map();accounts.filter(a=>!String(a.code).startsWith('607')).forEach(a=>{const key=category(a.code);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(a)});
  const categoryOrder=['Employees Benefits','Travel Costs','Depreciation and Amortization','Maintenance cost','A&P, Marketing Activities','IT and Connectivity Expenses','Professional & Consultation Expenses','Utilities Expenses','Insurance Expenses','Logistic Expenses','Governmental and Taxes Expenses','Vehicles Expenses','Products related Expense','Other Expenses'];
  const detailRows=[],groupRows=[],setCalculatedFormulas=(row,start,end)=>{const r=row.number,sumAt=c=>{const letter=op.getColumn(c).letter;row.getCell(c).value={formula:`SUM(${letter}${start}:${letter}${end})`}};[5,6,7,8,10,...Array.from({length:12},(_,i)=>11+i)].forEach(sumAt);row.getCell(9).value={formula:`G${r}+H${r}`};row.getCell(23).value={formula:`SUM(K${r}:V${r})`};for(let c=5;c<=23;c++)row.getCell(c).numFmt=numberFormat};
  categoryOrder.filter(name=>groups.has(name)).forEach(groupName=>{
    const items=groups.get(groupName),groupValues=Array(24).fill(null);groupValues[2]=groupName;groupValues[3]=groupName;const groupRow=op.addRow(groupValues),groupNumber=groupRow.number,start=groupNumber+1;groupRows.push(groupNumber);
    groupRow.height=24;groupRow.eachCell({includeEmpty:true},cell=>{cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFFFFED0'}};cell.font={bold:true,color:{argb:'FF22363B'}};cell.border={bottom:{style:'thin',color:{argb:'FFE4EEEE'}}}});groupRow.getCell(3).font={bold:true,size:12,color:{argb:'FF18353B'}};
    items.forEach(a=>{const item=d.items?.[a.code],ref=referenceValues(item),landing=n(item?.landing),months=MONTHS.map((_,i)=>n(item?.newBudgetByMonth?.[`2027-${String(i+1).padStart(2,'0')}`])),row=op.addRow([d.cc,a.code,a.name,groupName,ref.ly,ref.budget,ref.actual,landing,null,ref.fy,...months,null,'Department Input']),r=row.number;row.getCell(9).value={formula:`G${r}+H${r}`};row.getCell(23).value={formula:`SUM(K${r}:V${r})`};row.height=22;detailRows.push(r);for(let c=1;c<=24;c++){const cell=row.getCell(c);cell.border={bottom:{style:'hair',color:{argb:'FFDCE9E7'}},right:{style:'hair',color:{argb:'FFE7F0EF'}}};cell.alignment={vertical:'middle'};if(c>=5&&c<=23)cell.numFmt=numberFormat}row.getCell(8).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFEAF9F5'}};row.getCell(8).protection={locked:false};row.getCell(9).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFDDF6ED'}};for(let c=11;c<=22;c++){row.getCell(c).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFEAF9F5'}};row.getCell(c).protection={locked:false}}row.getCell(23).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFDDF6ED'}};row.getCell(23).font={bold:true,color:{argb:'FF08734D'}}});
    const end=op.lastRow.number;setCalculatedFormulas(groupRow,start,end)
  });
  const totalValues=Array(24).fill(null);totalValues[2]='TOTAL';totalValues[3]='TOTAL';const totalRow=op.addRow(totalValues),trn=totalRow.number,sumGroups=c=>{const letter=op.getColumn(c).letter;totalRow.getCell(c).value={formula:`SUM(${groupRows.map(r=>`${letter}${r}`).join(',')})`}};[5,6,7,8,10,...Array.from({length:12},(_,i)=>11+i)].forEach(sumGroups);totalRow.getCell(9).value={formula:`G${trn}+H${trn}`};totalRow.getCell(23).value={formula:`SUM(K${trn}:V${trn})`};for(let c=5;c<=23;c++)totalRow.getCell(c).numFmt=numberFormat;totalRow.height=26;totalRow.eachCell({includeEmpty:true},cell=>{cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF082765'}};cell.font={bold:true,color:{argb:'FFFFFFFF'}};cell.border={top:{style:'medium',color:{argb:'FF35D8C7'}}}});
  [17,16,38,28,16,16,16,16,17,17,...Array(12).fill(13),18,25].forEach((w,i)=>op.getColumn(i+1).width=w);[1,4].forEach(c=>op.getColumn(c).hidden=true);op.views=[{state:'frozen',ySplit:2,xSplit:3}];op.autoFilter={from:{row:2,column:2},to:{row:2,column:24}};op.properties.defaultRowHeight=22;
  await op.protect('DAD-Budget-2027',{selectLockedCells:true,selectUnlockedCells:true,formatCells:false,formatColumns:false,formatRows:false,insertColumns:false,insertRows:false,deleteColumns:false,deleteRows:false,sort:false,autoFilter:true});
  op.getCell('A1').note=`Reference period: ${range.from} to ${range.to}. Enter Landing 2026 and Budget 2027 only in the green cells.`;

  const pr=wb.addWorksheet('Professional Consultation');pr.addRow(['PROFESSIONAL & CONSULTATION · LANDING 2026 & FY BUDGET 2027']);pr.mergeCells('A1:Y1');pr.getCell('A1').font={bold:true,size:16,color:{argb:'FFFFFFFF'}};pr.getCell('A1').fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF082765'}};pr.getRow(1).height=30;
  pr.addRow(['Fund Center','G/L Account','Account Name','Details','Category','LY YTD','Budget YTD','Actual','Landing','FY Landing','FY Budget 2026',...MONTHS,'FY Budget 2027','Source / Note']);const ph=pr.getRow(2);ph.height=30;ph.eachCell(cell=>{cell.font={bold:true,color:{argb:'FFFFFFFF'}};cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF0A2C61'}};cell.alignment={vertical:'middle',horizontal:'center',wrapText:true}});ph.getCell(4).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF087F77'}};ph.getCell(9).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF087F77'}};for(let c=12;c<=23;c++)ph.getCell(c).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF087F77'}};ph.getCell(24).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF0B665F'}};
  professionalAccounts.forEach(a=>{const item=d.items?.[a.code],ref=referenceValues(item),landing=n(item?.landing),months=MONTHS.map((_,i)=>n(item?.newBudgetByMonth?.[`2027-${String(i+1).padStart(2,'0')}`])),row=pr.addRow([d.cc,a.code,a.name,item?.professionalDetails||'','Professional & Consultation Expenses',ref.ly,ref.budget,ref.actual,landing,null,ref.fy,...months,null,'Department Input']),r=row.number;row.getCell(10).value={formula:`H${r}+I${r}`};row.getCell(24).value={formula:`SUM(L${r}:W${r})`};for(let c=1;c<=25;c++){const cell=row.getCell(c);cell.border={bottom:{style:'hair',color:{argb:'FFDCE9E7'}},right:{style:'hair',color:{argb:'FFE7F0EF'}}};if(c>=6&&c<=24)cell.numFmt=numberFormat}[4,9,...Array.from({length:12},(_,i)=>12+i)].forEach(c=>{row.getCell(c).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFEAF9F5'}};row.getCell(c).protection={locked:false}});[10,24].forEach(c=>row.getCell(c).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FFDDF6ED'}})});[17,16,38,48,28,16,16,16,16,17,17,...Array(12).fill(13),18,25].forEach((w,i)=>pr.getColumn(i+1).width=w);[1,5].forEach(c=>pr.getColumn(c).hidden=true);pr.views=[{state:'frozen',ySplit:2,xSplit:4}];pr.autoFilter={from:{row:2,column:2},to:{row:2,column:25}};await pr.protect('DAD-Budget-2027',{selectLockedCells:true,selectUnlockedCells:true,autoFilter:true});

  const tr=wb.addWorksheet('Travel Budget');const headers=['Employee Number','Fund Center','Employee Name','Title','From City','Destination','Reason','Month','number of nights','Travel Tickets JD','Travel Hotels JD','Travel Transportation JD','Travel Meals JD','Travel Visa JD','Travel Per Diem JD','Travel Insurance JD','Local Per Diem JD','Local Transportaion JD','Other Travel Cost JD'];tr.addRow(headers);for(let i=0;i<40;i++)tr.addRow([null,d.cc,null,null,null,null,null,null,null,...Array(10).fill(null)]);[16,22,24,20,18,18,28,12,16,...Array(10).fill(20)].forEach((w,i)=>tr.getColumn(i+1).width=w);tr.views=[{state:'frozen',ySplit:1}];
  const titles=['C Level','Director','Manager','Other'],nights=Array.from({length:20},(_,i)=>i+1),lists=wb.addWorksheet('Lists');lists.state='veryHidden';lists.addRow(['Countries','Departments','Months','Titles','Number of Nights']);COUNTRIES.forEach((v,i)=>lists.getCell(i+2,1).value=v);const deps=Object.values(m.departments||{}).sort((a,b)=>String(a.name).localeCompare(String(b.name)));deps.forEach((v,i)=>lists.getCell(i+2,2).value=v.name||v.cc);MONTHS.forEach((v,i)=>lists.getCell(i+2,3).value=v);titles.forEach((v,i)=>lists.getCell(i+2,4).value=v);nights.forEach((v,i)=>lists.getCell(i+2,5).value=v);
  const countryFormula=`Lists!$A$2:$A$${COUNTRIES.length+1}`,deptFormula=`Lists!$B$2:$B$${deps.length+1}`,monthFormula='Lists!$C$2:$C$13',titleFormula='Lists!$D$2:$D$5',nightsFormula='Lists!$E$2:$E$21';for(let r=2;r<=41;r++){tr.getCell(r,2).dataValidation={type:'list',allowBlank:false,formulae:[deptFormula],showErrorMessage:true,errorTitle:'Select Department',error:'Please select a department from the list.'};tr.getCell(r,4).dataValidation={type:'list',allowBlank:false,formulae:[titleFormula],showErrorMessage:true,errorTitle:'Select Title',error:'Please select a title from the list.'};tr.getCell(r,5).dataValidation={type:'list',allowBlank:false,formulae:[countryFormula],showErrorMessage:true,errorTitle:'Select Country',error:'Please select a country from the list.'};tr.getCell(r,6).dataValidation={type:'list',allowBlank:false,formulae:[countryFormula],showErrorMessage:true,errorTitle:'Select Country',error:'Please select a country from the list.'};tr.getCell(r,8).dataValidation={type:'list',allowBlank:false,formulae:[monthFormula],showErrorMessage:true,errorTitle:'Select Month',error:'Please select a month from the list.'};tr.getCell(r,9).dataValidation={type:'list',allowBlank:false,formulae:[nightsFormula],showErrorMessage:true,errorTitle:'Select Number of Nights',error:'Please select a number from 1 to 20.'}}
  [info,op,pr,tr].forEach(ws=>{ws.getRow(1).font={bold:true,color:{argb:'FFFFFFFF'}};ws.getRow(1).fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF0A2C61'}};ws.getRow(1).alignment={vertical:'middle'};if(ws===tr)ws.autoFilter={from:{row:1,column:1},to:{row:1,column:ws.columnCount}}});
  for(let c=10;c<=19;c++){const cell=tr.getRow(1).getCell(c);cell.fill={type:'pattern',pattern:'solid',fgColor:{argb:'FF9DD7F5'}};cell.font={bold:true,color:{argb:'FF0A2C61'}};cell.alignment={vertical:'middle',horizontal:'center'}}
  const buf=await wb.xlsx.writeBuffer(),blob=new Blob([buf],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`Budget_2027_${String(d.name).replace(/[^A-Za-z0-9]+/g,'_')}_${d.cc}.xlsx`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);
}
function install(){const b=document.getElementById('masterTemplateBtn');if(!b)return setTimeout(install,250);b.onclick=downloadAllRows;b.dataset.allRowsTemplate='1'}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();window.addEventListener('load',()=>setTimeout(install,0));
