import {getApps} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import {getAuth,onAuthStateChanged} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import {getFirestore,doc,getDoc,onSnapshot} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js';
const app=getApps()[0],auth=getAuth(app),db=getFirestore(app),MAIN='PST3chwdZmaQGeG25t4ym9Vlixe2';
const clean=v=>String(v??'').trim();let profile=null,unsub=null,currentCc='';
function isAdmin(){return auth.currentUser?.uid===MAIN||profile?.role==='admin'||profile?.isMainAdmin===true}
function controls(){return{upload:document.getElementById('opexUploadBtn'),input:document.getElementById('opexUploadInput'),submit:[...document.querySelectorAll('button')].find(b=>clean(b.textContent)==='Submit Budget')}}
function ensureNote(){let n=document.getElementById('opexApprovalLockNote');if(n)return n;const bar=document.querySelector('.sheet-toolbar');if(!bar)return null;n=document.createElement('div');n.id='opexApprovalLockNote';n.style.cssText='display:none;margin:0 16px 10px;padding:9px 12px;border-radius:8px;background:#fff0ee;border:1px solid #f2c8c3;color:#9e2f2f;font-size:10px;font-weight:1000;box-shadow:0 0 10px rgba(178,59,59,.08)';n.textContent='Approved by Finance · OPEX is locked. Finance must return it before another upload or edit.';bar.insertAdjacentElement('afterend',n);return n}
function applyLocked(locked){const c=controls(),n=ensureNote();if(isAdmin())locked=false;[c.upload,c.submit].forEach(b=>{if(!b)return;b.disabled=!!locked;b.style.opacity=locked?'.45':'';b.style.cursor=locked?'not-allowed':''});if(c.input)c.input.disabled=!!locked;if(n)n.style.display=locked?'block':'none';document.body.dataset.opexApprovedLock=locked?'1':'0'}
async function profileFor(user){try{const s=await getDoc(doc(db,'users',user.uid));profile=s.exists()?s.data()||{}:{}}catch(_){profile={}}}
function watch(cc){cc=clean(cc);if(unsub){unsub();unsub=null}currentCc=cc;if(!cc||isAdmin()){applyLocked(false);return}unsub=onSnapshot(doc(db,'opex_budget_submissions',cc),s=>{const st=s.exists()?clean(s.data()?.workflowStatus):'';applyLocked(st==='approved')},()=>applyLocked(false))}
function selected(){return clean(document.getElementById('deptFilter')?.value)}
function installGuards(){const c=controls();if(c.upload&&!c.upload.dataset.approvalGuard){c.upload.dataset.approvalGuard='1';c.upload.addEventListener('click',e=>{if(document.body.dataset.opexApprovedLock==='1'){e.preventDefault();e.stopImmediatePropagation();alert('This OPEX budget is approved and locked. Finance must return it before you can upload a new workbook.')}},true)}if(c.input&&!c.input.dataset.approvalGuard){c.input.dataset.approvalGuard='1';c.input.addEventListener('change',e=>{if(document.body.dataset.opexApprovedLock==='1'){e.preventDefault();e.stopImmediatePropagation();c.input.value='';alert('This OPEX budget is approved and locked. Finance must return it first.')}},true)}}
async function start(user){await profileFor(user);installGuards();watch(selected());const sel=document.getElementById('deptFilter');if(sel&&!sel.dataset.approvalLock){sel.dataset.approvalLock='1';sel.addEventListener('change',()=>watch(selected()))}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installGuards);else installGuards();
onAuthStateChanged(auth,u=>{if(u)start(u)});window.addEventListener('dad-user-ready',e=>{profile=e.detail?.profile||profile;if(e.detail?.user||auth.currentUser)start(e.detail?.user||auth.currentUser)});
