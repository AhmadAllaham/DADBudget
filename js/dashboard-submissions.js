import { getApps } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import { getAuth, onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import { getFirestore, collection, getDocs, doc, getDoc } from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js';
const app=getApps()[0],auth=getAuth(app),db=getFirestore(app),MAIN='PST3chwdZmaQGeG25t4ym9Vlixe2';
const weights={not_submitted:0,uploaded:25,submitted:45,under_review:70,returned:50,approved:100};
const labels={not_submitted:'Not Submitted',uploaded:'Uploaded',submitted:'Submitted',under_review:'Under Review',returned:'Returned',approved:'Approved'};
function statusOf(st,hasUpload){return st?.status||(hasUpload?'uploaded':'not_submitted')}
function ensureSection(){let s=document.getElementById('submissionStatusSection');if(s)return s;s=document.createElement('section');s.id='submissionStatusSection';s.className='card roadmap-preview';s.innerHTML='<div class="section-head"><div><span class="eyebrow">SUBMISSION STATUS</span><h2>Your Budget Workflow</h2><p id="submissionSummary" style="margin:4px 0 0;color:#71878b;font-size:11px">Loading...</p></div><a id="submissionAdminLink" class="teal-btn" href="submission-control.html" style="display:none;text-decoration:none;align-items:center">Submission Control</a></div><div id="submissionStatusGrid" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:8px;margin-top:12px"></div>';const road=document.querySelector('.roadmap-preview:last-of-type');road?.parentNode?.insertBefore(s,road);return s}
async function load(user){const ps=await getDoc(doc(db,'users',user.uid));if(!ps.exists())return;const p=ps.data()||{},admin=user.uid===MAIN||p.role==='admin'||p.isMainAdmin===true,s=ensureSection(),link=document.getElementById('submissionAdminLink');if(admin)link.style.display='inline-flex';const assigned=Array.isArray(p.departments)?p.departments.map(String).filter(x=>x&&x!=='ALL'):[];const [statusSnap,uploadSnap]=await Promise.all([getDocs(collection(db,'budget_submission_status')),getDocs(collection(db,'opex_budget_submissions'))]);const statuses={},uploads={};statusSnap.forEach(x=>statuses[x.id]=x.data());uploadSnap.forEach(x=>uploads[x.id]=x.data());let ids=assigned;if(admin){ids=[...new Set([...Object.keys(statuses),...Object.keys(uploads)])]}if(!ids.length){document.getElementById('submissionSummary').textContent=admin?'No department submissions yet.':'No departments assigned.';document.getElementById('submissionStatusGrid').innerHTML='';return}let total=0;const cards=ids.map(cc=>{const st=statusOf(statuses[cc],!!uploads[cc]),name=statuses[cc]?.departmentName||uploads[cc]?.name||cc;total+=weights[st]??0;return`<div style="border:1px solid #d9e9e6;border-radius:10px;padding:10px 12px;background:#fbfdfd"><div style="font-size:9px;color:#74898d;font-weight:900">${cc}</div><div style="font-size:12px;font-weight:1000;color:#173f46;margin-top:2px">${name}</div><div style="margin-top:8px;display:flex;justify-content:space-between;gap:8px;align-items:center"><span style="font-size:9px;font-weight:1000;color:#087a64">${labels[st]||st}</span><span style="font-size:9px;color:#74898d">${weights[st]??0}%</span></div><div style="height:6px;border-radius:999px;background:#e7efee;margin-top:6px;overflow:hidden"><div style="height:100%;width:${weights[st]??0}%;background:#12a397"></div></div>${statuses[cc]?.note?`<div style="font-size:8px;color:#7b8f92;margin-top:6px">${statuses[cc].note}</div>`:''}</div>`}).join('');const progress=Math.round(total/ids.length);document.getElementById('submissionStatusGrid').innerHTML=cards;document.getElementById('submissionSummary').textContent=`${ids.length} department(s) · Overall workflow progress ${progress}%`;const kpi=[...document.querySelectorAll('.kpi-card')].find(x=>x.querySelector('span')?.textContent.trim()==='Budget Progress');if(kpi){kpi.querySelector('strong').textContent=progress+'%';kpi.querySelector('small').textContent=ids.filter(cc=>['submitted','under_review','approved'].includes(statusOf(statuses[cc],!!uploads[cc]))).length+' / '+ids.length+' departments submitted'}}
onAuthStateChanged(auth,user=>{if(user)load(user).catch(e=>console.error('Dashboard submission status failed',e))});
window.addEventListener('dad-user-ready',e=>{const u=e.detail?.user||auth.currentUser;if(u)load(u).catch(e=>console.error('Dashboard submission status failed',e))});
