import {getApps,initializeApp} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-app.js';
import {getAuth,onAuthStateChanged} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-auth.js';
import {getFirestore,doc,getDoc,getDocs,collection,query,orderBy,startAt,endAt,documentId} from 'https://www.gstatic.com/firebasejs/12.16.0/firebase-firestore.js';

const firebaseConfig={apiKey:'AIzaSyDAMLbm1ngqtzKjnDp6AMz8ucyhqNSnfBY',authDomain:'budget-8c575.firebaseapp.com',projectId:'budget-8c575',storageBucket:'budget-8c575.firebasestorage.app',messagingSenderId:'990142203884',appId:'1:990142203884:web:5c22dc2c14855528a022c9'};
const MAIN_ADMIN_UID='PST3chwdZmaQGeG25t4ym9Vlixe2',DOC_PREFIX='subscription_budget_',CACHE_KEY='dadBudgetSubscriptionsPlansV1',CACHE_MS=10*60*1000;
const app=getApps().length?getApps()[0]:initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app);
const clean=v=>String(v??'').trim();
let directory=[],profile=null,busy=false,observer=null;

function cachedProfile(){try{const p=JSON.parse(localStorage.getItem('dadBudgetCurrentProfile')||'null');return p&&clean(p.uid)===clean(auth.currentUser?.uid)?p:null}catch(_){return null}}
function departmentsOf(p={}){const out=Array.isArray(p.departments)?p.departments.map(clean).filter(Boolean):[];if(p.department&&!out.includes(clean(p.department)))out.push(clean(p.department));return out}
function isAdmin(){const user=auth.currentUser;return !!user&&(user.uid===MAIN_ADMIN_UID||profile?.isMainAdmin===true||profile?.role==='admin')}
async function loadProfile(user){const local=cachedProfile();if(local)return local;const s=await getDoc(doc(db,'users',user.uid));return s.exists()?s.data()||{}:{}}
function fullCache(){try{const x=JSON.parse(sessionStorage.getItem(CACHE_KEY)||'null');return x&&x.scope==='all'&&Date.now()-Number(x.ts||0)<CACHE_MS?x:null}catch(_){return null}}
function saveFullCache(plans){try{sessionStorage.setItem(CACHE_KEY,JSON.stringify({ts:Date.now(),scope:'all',plans}))}catch(_){}}
async function preloadPlans(){if(fullCache())return;const q=query(collection(db,'system_status'),orderBy(documentId()),startAt(DOC_PREFIX),endAt(`${DOC_PREFIX}\uf8ff`)),s=await getDocs(q),plans={};s.docs.forEach(x=>{const cc=clean(x.id).slice(DOC_PREFIX.length);if(cc)plans[cc]=x.data()||{}});saveFullCache(plans)}
async function loadDirectory(){const s=await getDoc(doc(db,'opex_baseline_meta','current'));if(!s.exists())return[];const data=s.data()||{},raw=Array.isArray(data.departmentDirectory)?data.departmentDirectory:(Array.isArray(data.departments)?data.departments.map(cc=>({cc,name:cc})):[]);return raw.map(x=>({cc:clean(x.cc),name:clean(x.name||x.departmentName||x.cc)})).filter(x=>x.cc&&x.cc!=='16').sort((a,b)=>a.name.localeCompare(b.name)||a.cc.localeCompare(b.cc,undefined,{numeric:true}))}
function applyEditAccess(){const sel=document.getElementById('subscriptionDept'),upload=document.getElementById('subscriptionUpload'),download=document.getElementById('subscriptionDownload');if(!sel)return;const selected=clean(sel.value),allowed=new Set(departmentsOf(profile)),canEdit=isAdmin()||(selected&&selected!=='ALL'&&allowed.has(selected));[upload,download].forEach(btn=>{if(!btn)return;btn.disabled=!canEdit;btn.title=canEdit?'':'View only — select one of your assigned departments to edit or download its template.'});const status=document.getElementById('subscriptionStatus');if(status&&!canEdit&&selected)status.dataset.viewOnly='1';else status?.removeAttribute('data-view-only')}
function enforceAllDepartments(){const sel=document.getElementById('subscriptionDept');if(!sel||!directory.length||busy)return;busy=true;observer?.disconnect();try{const previous=clean(sel.value),existing=new Set([...sel.options].map(o=>o.value));if(!existing.has('ALL')){const all=document.createElement('option');all.value='ALL';all.textContent='All Departments';sel.insertBefore(all,sel.firstChild);existing.add('ALL')}directory.forEach(d=>{if(existing.has(d.cc))return;const o=document.createElement('option');o.value=d.cc;o.textContent=`${d.cc} · ${d.name}`;sel.appendChild(o)});if(previous&&[...sel.options].some(o=>o.value===previous))sel.value=previous;sel.disabled=false;applyEditAccess()}finally{busy=false;if(observer&&document.body.contains(sel))observer.observe(sel,{childList:true,subtree:true})}}
function watchSelect(){const sel=document.getElementById('subscriptionDept');if(!sel)return false;if(!observer){observer=new MutationObserver(()=>{if(!busy)setTimeout(enforceAllDepartments,0)});observer.observe(sel,{childList:true,subtree:true})}if(sel.dataset.allDepartmentsAccessBound!=='1'){sel.dataset.allDepartmentsAccessBound='1';sel.addEventListener('change',()=>setTimeout(applyEditAccess,0))}enforceAllDepartments();return true}
async function boot(user){if(!user)return;try{profile=await loadProfile(user);await Promise.all([preloadPlans(),loadDirectory().then(x=>directory=x)]);let tries=0;const timer=setInterval(()=>{tries++;if(watchSelect()||tries>60)clearInterval(timer)},100);window.addEventListener('load',()=>setTimeout(enforceAllDepartments,250))}catch(error){console.warn('Subscriptions all-departments preload skipped',error)}}
onAuthStateChanged(auth,boot);
